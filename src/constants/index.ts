import {
  APP_ROUTES,
  INITIAL_ENDPOINT,
  INITIAL_QUERY,
  ParamKeys,
  LANGUAGES,
  LangPages,
} from "./constants";
import { Query, QueryData } from "./types";

export {
  APP_ROUTES,
  INITIAL_ENDPOINT,
  INITIAL_QUERY,
  ParamKeys,
  LANGUAGES,
  LangPages,
};
export type { Query, QueryData };
